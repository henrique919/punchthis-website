# PunchThis Marketing — Design-Engineering Loop Ledger

Execution ledger for `PUNCHTHIS_DESIGN_UX_AUDIT.md` (19 July 2026). Not a prose diary — update rows in place as work actually lands. Claims are cross-checked against `PRODUCT_TRUTH.md`. Priority queue and acceptance bar are defined in the loop prompt supplied with the audit.

`ID | Priority | Finding | Evidence | Scope | Acceptance criteria | Tests | Status | Commit`

| ID | Priority | Finding | Evidence | Scope | Acceptance criteria | Tests | Status | Commit |
|---|---|---|---|---|---|---|---|---|
| P0-1 | P0 | Marketing claims a 5-state issue lifecycle (`Open→Assigned→In Progress→For Review→Verified`) and contractor sign-off; app ships only 4 states, single-actor | C-03 | `content.js` (BENEFITS, FAQ_ITEMS), `HowItWorks.jsx` (LIFECYCLE), `CaptureScreen.jsx`, `PDFPreviewScreen.jsx`, `IssueDetailScreen.jsx`, `ReportBuilderScreen.jsx`, `ReportsPage.jsx`, `Differentiation` copy | No page shows "For Review"/"Verified"/"Overdue" as a status; all lifecycle copy matches `PRODUCT_TRUTH.md` exactly | Repo-wide grep for banned strings (clean); `npm run build` clean | Complete | `7e2464b` (build-portability prerequisite: `e80a730`) |
| P0-2 | P0 | Early-access form opens `mailto:` — no real submission, no storage, exposes dev copy on success | C-01 | `EarlyAccess.jsx`, new `api/early-access.js`, new `src/lib/analytics.js` | Real POST endpoint with server validation, rate limiting, honeypot, accessible error/retry/success states, analytics events fired, no implementation details shown to visitor | Playwright: invalid-email inline error + `aria-invalid`, valid-email submit reaches honest error state (no local `/api` in dev) not a fake success, honeypot off-screen but not `display:none`, mobile fold check (email field top < 844px @390 width) | Complete — **Blocked, external input**: needs `EARLY_ACCESS_WEBHOOK_URL` set in Vercel project env vars (see commit message for accepted shapes) | `28ec804` |
| P0-3 | P0 | Sign in and legal footer links are `#`; no accounts exist; no Privacy/Terms/Support/Data-deletion routes | C-02 | `Header.jsx`, `Footer.jsx`, `content.js` (NAV/FOOTER), new legal route pages, `App.jsx` routes | Sign in removed (no destination exists); 4 legal routes published with real provisional copy sourced from app's `legalCopy.ts`; zero production `#` hrefs | Repo-wide grep for `href="#"`/`to="#"`/`signInUrl` (clean); Playwright screenshot of all 4 routes, 200 status, correct heading + provisional notice on each | Complete | `fb23b9a` |
| P1-4 | P1 | Live mobile horizontal overflow (375 clientWidth vs 436 scrollWidth) | H-02 | `Header.jsx`, `ReportsPage.jsx`, `global.css` | `scrollWidth === clientWidth` at 320/375/390px on every route (spot-checked; full 6-breakpoint x 11-route automated matrix was too slow for this environment's dev-server latency to run exhaustively in one pass) | Playwright per-element bounding-box diagnostic on all 11 routes at 320/375/390px, before/after; root causes fixed (header CTA sizing, CSS Grid `1fr`->`minmax(0,1fr)`, a dead/mistargeted media query removed); `overflow-x:clip` added as the audit's recommended safety guard | Complete | `9746c60` |
| P1-5 | P1 | `.reveal` starts at `opacity:0`, depends on IntersectionObserver; can hide entire page if JS/observer fails | H-03 | `global.css`, `useReveal.js` | Content visible by default; enhancement class applied via JS before hiding; observer timeout fallback; reduced-motion unaffected | Live-captured the bug first (fresh `/how-it-works` load showed ~2500px of blank space where WorkflowSection/AudienceSection should be); after fix, same test shows 0 of 15 reveal elements stuck at opacity:0 three seconds after load | Complete | `d01419b` |
| P1-6 | P1 | Simulated hand-drawn SVG screens stand in for real product proof | H-01, C-03 | `src/screens/*.jsx`, real capture pipeline, media manifest | At least the hero + one showcase use real captured evidence (seed photos / real screenshots), versioned and labelled | Built the real `audit-snap-663` web export (commit `6c3e202`) and drove it end-to-end with Playwright: dashboard -> capture session -> issue detail -> markup studio -> hit list -> report builder -> PDF preview, all real sample seed data. Swapped every *static* screen (Hero, WorkflowSection, ReportShowcase, Product, ReportsPage hero); left the two genuinely-interactive tool-switcher demos on SVG (documented why in the commit). Deleted 5 now-unused SVG components | Complete | `adab9e0` |
| P1-7 | P1 | Homepage repeats the same capture→markup→assign→report story 5+ times ("AI brochure" feel) | M-01, H-01 | `Home.jsx`, new `IssueJourney.jsx`, `BeforeAfterSlider.jsx`, `WhatShipsToday.jsx`, `Hero.jsx`, `ReportShowcase.jsx`, `content.js` | Proof-led sequence per M-01; one real issue shown end-to-end; two-level CTA present; dropped a fully-redundant section | Build clean; Playwright: keyboard slider control (ArrowRight x2 -> aria-valuenow 50->60), same-page anchor CTA scroll (scrollY 0->1448), 320/375px overflow clean, new green/amber title colors computed 5.37:1/5.38:1 on white | Complete | `cf02939` |
| P1-8 | P1 | Early-access email field is below the mobile fold; all fields required upfront | H-06, C-01 | `EarlyAccess.jsx` | Email capture immediately below promise on mobile; name/role collected progressively after success | Playwright: email input bounding-box top = 646px at 390×844 viewport (within first fold) | Complete (bundled with P0-2) | `28ec804` |
| P1-9 | P1 | Every route inherits homepage title/description/canonical/OG; `/og-image.png` missing; no 404 | H-04 | `index.html`, new `Seo.jsx`, `public/og-image.png`, `App.jsx` 404 route, `sitemap.xml` | Each route has distinct title/description/canonical/OG; real OG image present; unknown routes render a real 404 | Playwright per-route check of `document.title`/description/canonical across all 12 routes - each distinct and correct; real 1200x630 OG image rendered from the brand system + a real screenshot; conservative Organization JSON-LD (no fabricated ratings/pricing) | Complete — residual gap documented: no prerendering, so metadata only reaches JS-executing crawlers, and the 404 route still returns HTTP 200 (SPA fallback) rather than a true 404 status; both need prerendering/SSR, out of scope per the no-framework-rewrite rule | `5b64174` |
| P1-10 | P1 | White-on-cobalt buttons, steel body text and footer microcopy miss WCAG AA | H-08 | `global.css` tokens, `Footer.jsx` and 6 component files' text-colour usages | Button fill ≥4.5:1 with white label; body/caption text ≥4.5:1 on its surface | Computed exact (unrounded) WCAG contrast ratios for every old/new pair via a small script — see commit message; button: 3.53:1 to 6.13:1, steel-text: 3.49:1 to 5.41:1 (white)/4.91:1 (mist), footer: 2.63:1 to 6.72:1. Building the P2-17 axe-core CI gate with `reducedMotion:'reduce'` (removing an animation-timing false-positive source) then caught real misses the manual pass didn't: Logo "This" wordmark, `--muted` on `--mist`, `.chip-open`/`.chip-warn`, HowItWorks lifecycle pills, several cobalt-as-text hover/label spots — all fixed and reverified to 0 axe violations across 11 routes | Complete — residual gap documented (SVG mockup internal text, deferred to P1-6) | `c8d575b`, `e125b8e` |
| P1-11 | P1 | No skip link; 40px mobile menu button; no `aria-invalid`/error summary on form | H-08 (low-priority polish), C-01 | `App.jsx`, `Header.jsx`, `global.css`, `MarkupShowcase.jsx` | Skip-to-content link present and functional; primary interactive mobile targets ≥44px; form has `aria-invalid` + accessible error summary | Keyboard walkthrough (Tab -> visible skip link -> Enter -> focus lands on `<main>`); automated bounding-box sweep of every button/link on 8 routes at 390px, which also surfaced and fixed two contrast misses P1-10 didn't catch (see commit) | Complete — `aria-invalid`/error summary already shipped with P0-2; footer link-list rows (16px tall, standard stacked-link density) and inline text links intentionally left below 44px, see commit message | `e6e1926` |
| P2-12 | P2 | Visual rules scattered across inline `<style>` blocks per component | M-03 | New `STYLE_GUIDE.md` | Button/chip/section/card/form/nav variants and states documented; breakpoint scale documented; explicit rationale for keeping component-scoped `<style>` blocks over a CSS Modules migration | Cross-checked every documented pattern against the real codebase (not just described intent) — surfaced and recorded real drift: 3/5 `.chip-*` variants never rendered, the shared `dark-hero` convention isn't actually consistent across its 6 call sites (ReportsPage uses `--slate` not `--ink`, LegalLayout uses shorter padding, MarkupPage's `dark-hero` class is inert), `.eyebrow` down to 1 real use vs. 13 for `.section-label`; `check:links`/`check:claims`/`check:tokens`/build all still pass (docs-only, no `src/` changes) | Complete — drift found while documenting is recorded as a follow-up, not silently fixed (would be a visual change without confirmed intent) | `5c32961` |
| P2-13 | P2 | No shared brand-token source between marketing site and Expo app | Handoff §2 | New `src/config/brand-tokens.mjs`, `scripts/check-token-drift.mjs`, `global.css` (4 new status-text vars), 3 components de-duped onto them | Every website CSS custom property mapped to its app `palette` source (app repo, commit, file) with match/diverge/website-only notes; CI fails on undocumented drift | `check:tokens` passes locally (27/27 tokens matched); full a11y+overflow scan re-run after the var-for-hex swap, still 0 violations / 0 overflow across 11 routes x 6 breakpoints | Complete — no cross-repo build-time codegen (separate repos/bundlers, out of scope); radius/spacing scale divergences between app and website documented but intentionally not reconciled (visible layout change, disproportionate to this item) | `73c1033` |
| P2-14 | P2 | No versioned media manifest for real product screenshots | M-04 | `src/config/media-manifest.js` (shipped under P1-6), new `scripts/check-media-manifest.mjs` | Every real media asset records source app version/commit, route, capture date, alt text; CI fails if the manifest and `src/assets/app-screens/` drift in either direction | `check:media` passes locally (9/9 entries matched); build clean | Complete — manifest itself landed in `adab9e0` (P1-6) but was never marked complete or given the CI enforcement half of M-04's recommendation ("fail CI if required marketing assets are missing") until this pass | `adab9e0` (manifest), `94b4b95` (CI check) |
| P2-15 | P2 | No code splitting / perf budget; all SVG screens ship on first load | M-06 | `App.jsx`, `main.jsx`, `PhoneMockup.jsx`, `Hero.jsx`, new `PERFORMANCE.md` | Route-level chunks; budgets documented | Build output (home chunk 74.7KB gzip, well under 150KB budget); zero failed font requests after self-hosting (was failing every load); LCP-candidate images switched from lazy to eager+high-priority | Complete — LCP/CLS/INP not measured (no Lighthouse-class tooling in this environment), documented in PERFORMANCE.md | `ef9d62c` |
| P2-16 | P2 | No analytics/experiment contract | M-05, C-01 | New analytics helper | Documented event names + properties; consent-aware no-op by default | Code review (no tracking script bundled; pushes to `dataLayer` only) | Complete (bundled with P0-2) | `28ec804` |
| P2-17 | P2 | No CI gates for build/links/metadata/claims/a11y/responsive/visual regression | Handoff §6 | `.github/workflows/ci.yml`, `scripts/check-links.mjs`, `scripts/check-claims.mjs`, `scripts/check-a11y-and-overflow.mjs` | CI runs build + dead-link + banned-claim checks on every PR/push to `main`, then serves the production build and runs an axe-core WCAG2A/AA + 6-breakpoint overflow sweep across all 11 routes, uploading screenshots as an artifact | Ran the full pipeline locally against the production build (`npm run build && vite preview` + each script): links/claims clean; a11y+overflow run iterated until 0 violations (see P1-10) — final local run: "PASS: no overflow at 320/375/390/768/1024/1440px and no WCAG 2 A/AA violations across 11 routes" | Complete | `823317f` |
| P2-18 | P2 | App's wide web layouts stretch to full viewport with no desktop shell | H-07 | `audit-snap-663`: new `layout.webMaxWidth` token, dashboard/project/capture-session/issue-detail screens (see repo PR #12) | Operational content constrained to a deliberate max width; native unaffected | `tsc --noEmit` clean; full jest suite 30/30 suites, 222/222 tests passing; Playwright walkthrough of the built web export at 1600×900 with real seeded demo data through all four named screens, screenshots reviewed manually | Complete — capture-session needed a different fix shape (dark full-bleed chrome + capped inner content, not a capped outer box) after a first attempt produced a visible light seam around the dark panel, caught by screenshot before committing; `audit/[id]/hitlist.tsx` has the same issue but wasn't one of the four screens the audit named, left as a documented follow-up | `audit-snap-663@3c38587` (PR [#12](https://github.com/henrique919/audit-snap-663/pull/12)) |

## Notes

- A row only moves to `Complete` after the change is actually committed. The commit column holds the real hash from `git log` at that moment — never a placeholder or a guess.
- "Blocked — external input" rows are implemented end-to-end except for the one credential/decision only the operator can supply; that value is documented at the point of blockage, and the loop continues to the next item without waiting.

## Round 2 — stakeholder design feedback (post-completion)

The 18-item queue above closed out the original written audit. This section tracks a live follow-up round of direct feedback from the product owner, reviewing the actual rendered site rather than the audit document.

| ID | Finding | Scope | Status | Commit |
|---|---|---|---|---|
| R2-1 | Hero phone mockups showed a black bar over real header text ("Markup Studio", "Pre-Handover Site Walk") | `PhoneMockup.jsx` — root cause was a decorative notch overlay colliding with real header text (web captures have no safe-area gap a real notch would clear) | Complete | `8f1c4ce` |
| R2-2 | Markup demo photo showed 7 stacked annotation marks at once ("markups over markups") | `audit-snap-663` issue #008 seed annotations trimmed 8→3; `IssueJourney.jsx` copy updated to match; screenshots re-captured | Complete | `audit-snap-663@ec6b0a1`, `8f1c4ce` |
| R2-3 | "Only what's actually built. Nothing implied." section read as a defensive disclaimer, not a selling point | `WhatShipsToday.jsx` — reframed headline/labels/icon to "Built, not promised." / Live now / Beta / Coming next; underlying claims unchanged | Complete | `1314c8a` |
| R2-4 | Seed "photos" behind the phone mockups are synthetic gradient placeholders (`scripts/generate-seed-photos.mjs`), not real photos — described as "putrid" | All 9 seed photos (cover + 8 issues) replaced with real photos in `audit-snap-663` (4 operator-supplied, 2 from Unsplash); re-captured every affected marketing screenshot; PNG screenshots switched to JPEG q78 to avoid a ~150x bundle-size regression (~10.5MB → ~1MB for 9 files) | Complete — delivered via `henrique919/punchthis-website` PR #2 ("Photos for Claude"); `tsc`/jest clean in the app repo, `check:media` + full a11y/overflow scan (0 violations, 11 routes × 6 breakpoints) clean on the website | `audit-snap-663@409ff0e`, `47b9569` |
| R2-5 | Site framing should stay usable/inclusive beyond construction specifically (siteauditpro.com referenced as inspiration, not a direction) | No immediate scope — noted for future consideration per the operator's own framing | Noted, no action taken | — |
