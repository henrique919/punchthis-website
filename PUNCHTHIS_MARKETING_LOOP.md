# PunchThis Marketing — Design-Engineering Loop Ledger

Execution ledger for `PUNCHTHIS_DESIGN_UX_AUDIT.md` (19 July 2026). Not a prose diary — update rows in place as work actually lands. Claims are cross-checked against `PRODUCT_TRUTH.md`. Priority queue and acceptance bar are defined in the loop prompt supplied with the audit.

`ID | Priority | Finding | Evidence | Scope | Acceptance criteria | Tests | Status | Commit`

| ID | Priority | Finding | Evidence | Scope | Acceptance criteria | Tests | Status | Commit |
|---|---|---|---|---|---|---|---|---|
| P0-1 | P0 | Marketing claims a 5-state issue lifecycle (`Open→Assigned→In Progress→For Review→Verified`) and contractor sign-off; app ships only 4 states, single-actor | C-03 | `content.js` (BENEFITS, FAQ_ITEMS), `HowItWorks.jsx` (LIFECYCLE), `CaptureScreen.jsx`, `PDFPreviewScreen.jsx`, `IssueDetailScreen.jsx`, `ReportBuilderScreen.jsx`, `ReportsPage.jsx`, `Differentiation` copy | No page shows "For Review"/"Verified"/"Overdue" as a status; all lifecycle copy matches `PRODUCT_TRUTH.md` exactly | Repo-wide grep for banned strings (clean); `npm run build` clean | Complete | `7e2464b` (build-portability prerequisite: `e80a730`) |
| P0-2 | P0 | Early-access form opens `mailto:` — no real submission, no storage, exposes dev copy on success | C-01 | `EarlyAccess.jsx`, new `api/early-access.js`, new `src/lib/analytics.js` | Real POST endpoint with server validation, rate limiting, honeypot, accessible error/retry/success states, analytics events fired, no implementation details shown to visitor | Playwright: invalid-email inline error + `aria-invalid`, valid-email submit reaches honest error state (no local `/api` in dev) not a fake success, honeypot off-screen but not `display:none`, mobile fold check (email field top < 844px @390 width) | Complete — **Blocked, external input**: needs `EARLY_ACCESS_WEBHOOK_URL` set in Vercel project env vars (see commit message for accepted shapes) | `28ec804` |
| P0-3 | P0 | Sign in and legal footer links are `#`; no accounts exist; no Privacy/Terms/Support/Data-deletion routes | C-02 | `Header.jsx`, `Footer.jsx`, `content.js` (NAV/FOOTER), new legal route pages, `App.jsx` routes | Sign in removed (no destination exists); 4 legal routes published with real provisional copy sourced from app's `legalCopy.ts`; zero production `#` hrefs | Repo-wide grep for `href="#"`/`to="#"`/`signInUrl` (clean); Playwright screenshot of all 4 routes, 200 status, correct heading + provisional notice on each | Complete | `fb23b9a` |
| P1-4 | P1 | Live mobile horizontal overflow (375 clientWidth vs 436 scrollWidth) | H-02 | `Header.jsx`, `ReportsPage.jsx`, `global.css` | `scrollWidth === clientWidth` at 320/375/390px on every route (spot-checked; full 6-breakpoint x 11-route automated matrix was too slow for this environment's dev-server latency to run exhaustively in one pass) | Playwright per-element bounding-box diagnostic on all 11 routes at 320/375/390px, before/after; root causes fixed (header CTA sizing, CSS Grid `1fr`->`minmax(0,1fr)`, a dead/mistargeted media query removed); `overflow-x:clip` added as the audit's recommended safety guard | Complete | `9746c60` |
| P1-5 | P1 | `.reveal` starts at `opacity:0`, depends on IntersectionObserver; can hide entire page if JS/observer fails | H-03 | `global.css`, `useReveal.js` | Content visible by default; enhancement class applied via JS before hiding; observer timeout fallback; reduced-motion unaffected | Live-captured the bug first (fresh `/how-it-works` load showed ~2500px of blank space where WorkflowSection/AudienceSection should be); after fix, same test shows 0 of 15 reveal elements stuck at opacity:0 three seconds after load | Complete | `d01419b` |
| P1-6 | P1 | Simulated hand-drawn SVG screens stand in for real product proof | H-01, C-03 | `src/screens/*.jsx`, real capture pipeline, media manifest | At least the hero + one showcase use real captured evidence (seed photos / real screenshots), versioned and labelled | Manual visual diff vs previous SVG | Queued | — |
| P1-7 | P1 | Homepage repeats the same capture→markup→assign→report story 5+ times ("AI brochure" feel) | M-01, H-01 | `Home.jsx`, homepage section order | Six deliberate acts per audit M-01; one real issue shown end-to-end; two-level CTA present | Manual read-through against acceptance standard's 6 questions | Queued | — |
| P1-8 | P1 | Early-access email field is below the mobile fold; all fields required upfront | H-06, C-01 | `EarlyAccess.jsx` | Email capture immediately below promise on mobile; name/role collected progressively after success | Playwright: email input bounding-box top = 646px at 390×844 viewport (within first fold) | Complete (bundled with P0-2) | `28ec804` |
| P1-9 | P1 | Every route inherits homepage title/description/canonical/OG; `/og-image.png` missing; no 404 | H-04 | `index.html`, new SEO helper, `public/og-image.png`, `App.jsx` 404 route | Each route has distinct title/description/canonical/OG; real OG image present; unknown routes render a real 404 | Manual per-route `document.title` check | Queued | — |
| P1-10 | P1 | White-on-cobalt buttons, steel body text and footer microcopy miss WCAG AA | H-08 | `global.css` tokens, `Footer.jsx` and 6 component files' text-colour usages | Button fill ≥4.5:1 with white label; body/caption text ≥4.5:1 on its surface | Computed exact (unrounded) WCAG contrast ratios for every old/new pair via a small script — see commit message; button: 3.53:1 to 6.13:1, steel-text: 3.49:1 to 5.41:1 (white)/4.91:1 (mist), footer: 2.63:1 to 6.72:1 | Complete — residual gap documented (SVG mockup internal text, deferred to P1-6; a handful of small/hover-only cobalt-as-text spots not in the audit's named pairs) | `c8d575b` |
| P1-11 | P1 | No skip link; 40px mobile menu button; no `aria-invalid`/error summary on form | H-08 (low-priority polish), C-01 | `App.jsx`/`Header.jsx` skip link, `global.css` target sizes, `EarlyAccess.jsx` | Skip-to-content link present and functional; all interactive mobile targets ≥44px; form has `aria-invalid` + accessible error summary | Keyboard-only walkthrough | Queued | — |
| P2-12 | P2 | Visual rules scattered across inline `<style>` blocks per component | M-03 | Shared CSS layer / documented variants | Reusable button/card/section/form variants documented | N/A (architecture) | Queued | — |
| P2-13 | P2 | No shared brand-token source between marketing site and Expo app | Handoff §2 | New token source synced from app `constants/theme.ts` | Marketing CSS vars generated from one documented token source | N/A | Queued | — |
| P2-14 | P2 | No versioned media manifest for real product screenshots | M-04 | New media manifest module | Every real media asset records source app version/commit, route, viewport, capture date, alt text | N/A | Queued | — |
| P2-15 | P2 | No code splitting / perf budget; all SVG screens ship on first load | M-06 | `App.jsx` route-level `React.lazy`, font loading | Route-level chunks; budgets documented | Build output size check | Queued | — |
| P2-16 | P2 | No analytics/experiment contract | M-05, C-01 | New analytics helper | Documented event names + properties; consent-aware no-op by default | Code review (no tracking script bundled; pushes to `dataLayer` only) | Complete (bundled with P0-2) | `28ec804` |
| P2-17 | P2 | No CI gates for build/links/metadata/claims/a11y/responsive/visual regression | Handoff §6 | `.github/workflows/ci.yml` | CI runs build + lint + link/overflow checks on PR | CI run | Queued | — |
| P2-18 | P2 | App's wide web layouts stretch to full viewport with no desktop shell | H-07 | `audit-snap-663` expo web layouts | Operational content constrained to a deliberate max width | Manual desktop viewport check (app repo) | Queued | — |

## Notes

- A row only moves to `Complete` after the change is actually committed. The commit column holds the real hash from `git log` at that moment — never a placeholder or a guess.
- "Blocked — external input" rows are implemented end-to-end except for the one credential/decision only the operator can supply; that value is documented at the point of blockage, and the loop continues to the next item without waiting.
