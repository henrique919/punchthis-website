// ─── PunchThis — Legal / disclosure copy ───────────────────────────────────
// Sourced verbatim from the app's own disclosure module
// (audit-snap-663 `expo/lib/legalCopy.ts`, screen `expo/app/data-privacy.tsx`)
// so the website never states anything the product itself doesn't already
// say. Keep this file's wording byte-identical to the app source — if the
// app copy changes, update here in the same change.

export const PROVISIONAL_NOTICE = 'Provisional wording pending legal review.'

export const LAST_UPDATED = '19 July 2026'

export const PRODUCT_SCOPE =
  'PunchThis is a documentation and workflow tool. It does not certify regulatory compliance, workmanship, safety, completion, or contractual performance. Users remain responsible for professional judgment, verification, and distribution of records.'

export const LOCAL_STORAGE_WARNING =
  'Projects, photos, and reports are stored only on your device — there is no backup elsewhere. Losing the device, deleting the app, or clearing data may permanently remove them. Export important records regularly.'

export const CAMERA_PHOTOS =
  'PunchThis uses your camera and selected photos to attach site evidence to inspection items. Content stays on your device unless you choose to share or email it.'

export const BLUR_REDACTION =
  'Blur is permanently applied to generated/exported copies. The original source photo remains on your device until you delete it.'

export const RETENTION_DELETION =
  'Clear all data (Settings → Clear all data, in the app) immediately deletes your projects, audits, and issue records, along with the photo, report, and brand files owned by the app on your device. Reports or photos you already exported or shared — for example by email or messaging — live outside the app and are not affected; deleting them there is up to you.'

export const EXPORT_SHARING =
  "Exporting or sharing a report or photo sends a copy through the app you choose — email, messaging, or another app on your device. Once it leaves PunchThis, that copy is outside the app's control and inherits the security and retention of the recipient channel."

export const NO_ACCOUNTS =
  'PunchThis does not currently have user accounts or sign-in. There is nothing stored on a server tied to your identity, and no account to delete — the data covered by this page is entirely local to your device.'

export const SUPPORT_INTRO =
  "PunchThis is in early access and under active development. If something looks wrong, breaks, or you just want to ask a question, we want to hear about it."
